import React, { useState } from "react"
import axios from "axios"
import useLocalStorage from "../../hooks/useLocalStorage"
import { useNavigate, Link } from "react-router-dom"

const PasswordRefresh = () => {
    return (
        <h1>
            Well, some things just are lost to time. Our team is working hard to get the password refresh feature working. For the moment, feel free to make a new account.
        </h1>
    )
}

export default PasswordRefresh

